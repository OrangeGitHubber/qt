<?xml version="1.0"?>
<Container version="2">
  <Name>qt-autotrader</Name>
  <Repository>ghcr.io/orangegithubber/qt:latest</Repository>
  <Registry>https://ghcr.io</Registry>
  <Network>bridge</Network>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/OrangeGitHubber/qt/issues</Support>
  <Project>https://github.com/OrangeGitHubber/qt</Project>
  <Overview>QT Auto-Trader: self-hosted momentum trading bot for stocks and crypto via Alpaca. Paper-trading first, all configuration through the web UI. GPLv3.</Overview>
  <Category>Tools:</Category>
  <WebUI>http://[IP]:[PORT:8420]/</WebUI>
  <Icon>https://raw.githubusercontent.com/OrangeGitHubber/qt/main/frontend/public/icon.png</Icon>
  <Config Name="WebUI Port" Target="8420" Default="8420" Mode="tcp" Description="Port for the QT web interface" Type="Port" Display="always" Required="true" Mask="false">8420</Config>
  <Config Name="Data" Target="/data" Default="/mnt/user/appdata/qt-autotrader" Mode="rw" Description="DIRECTION MATTERS: Host Path = a folder on your server (e.g. /mnt/user/appdata/qt-autotrader); Container Path must be exactly /data. Do NOT swap them — an inverted mapping makes the app write to throwaway storage and you lose config, keys and trade history on the next update. This folder holds the database, encrypted API keys and trade history. BACK IT UP. See docs/data-persistence.md." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/qt-autotrader</Config>
  <Config Name="Allow Leverage (DANGEROUS)" Target="QT_ALLOW_LEVERAGE" Default="false" Mode="" Description="Leave false. Setting true does NOT enable leverage - it only makes the option visible inside the app, where enabling still requires typed confirmation. Leverage multiplies losses; see docs/how-it-works.md." Type="Variable" Display="advanced" Required="false" Mask="false">false</Config>
  <Config Name="Bar Cache URL (optional)" Target="QT_BAR_CACHE_URL" Default="" Mode="" Description="Optional. Leave blank to use a local SQLite bar cache in /data (the default - zero setup). Set to a Postgres DSN (postgresql://user:password@HOST:5432/qt_bars) for a durable, shareable cache of historical market data. HOST is your server's LAN IP or the Postgres container name - never localhost. URL-encode special characters in the password (@ = %40, # = %23, / = %2F). The cache holds only public market data, never keys or journal. See docs/bar-cache.md." Type="Variable" Display="advanced" Required="false" Mask="true"></Config>
</Container>
